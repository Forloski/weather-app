import { Fab } from "@mui/material";
import { styled } from "@mui/system";

export const HomeFab = styled(Fab)(() => ({
  top: 20,
  right: "auto",
  bottom: "auto",
  left: 20,
  position: "fixed",
}));
